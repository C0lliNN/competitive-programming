#include <bits/stdc++.h>

using namespace std;

int main() {
  string name;
  int age;

  // The following two lines make input/output more efficient
  ios::sync_with_stdio(0);
  cin.tie(0);
  cin >> name >> age;
  cout << name << " " << age << "\n";

  return 0;
}